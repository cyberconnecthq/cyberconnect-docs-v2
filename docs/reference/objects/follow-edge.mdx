---
id: follow-edge
title: FollowEdge
---


The `FollowEdge` type provides a `Follow` node along with its cursor.

```graphql
type FollowEdge {
  node: Follow
  cursor: Cursor!
}
```


### Fields

#### `node` ([`Follow`](/reference/objects/follow))

`node` the node of the `Follow` object.


#### `cursor` ([`Cursor`](/reference/scalars/cursor))

`cursor` the cursor of the `Follow` node.




