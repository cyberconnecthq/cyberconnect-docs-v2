---
id: register-signing-key
title: registerSigningKey
---


`registerSigningKey` registers a signing key to use with `follow` and `unfollow` inputs. This mutation requires an `Authorization` header with a valid `Bearer` token.

```graphql
registerSigningKey(
  input: RegisterSigningKeyInput!
): RegisterKeyResponse!
```


### Arguments

#### `input` ([`RegisterSigningKeyInput`](/cyberconnect/reference/inputs/register-signing-key-input))




### Type

#### [`RegisterKeyResponse`](/cyberconnect/reference/objects/register-key-response) 

The `RegisterKeyResponse` type returns `registerSigningKey` request status.




