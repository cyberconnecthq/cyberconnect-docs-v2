---
id: blockchain-time
title: BlockchainTime
---


The `BlockchainTime` type provides block information.

```graphql
type BlockchainTime {
  blockNumber: Int!
  blocksBehind: Int!
}
```


### Fields

#### `blockNumber` ([`Int`](/cyberconnect/reference/scalars/int))

`blockNumber` the last synced block number.


#### `blocksBehind` ([`Int`](/cyberconnect/reference/scalars/int))

`blocksBehind` the number of blocks behind the latest block.




