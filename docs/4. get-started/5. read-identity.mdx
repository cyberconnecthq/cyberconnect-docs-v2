---
id: read-identity
title: Read Identity
slug: /get-started/read-identity/
sidebar_label: Read Identity
sidebar_position: 5
---

In this section you'll learn how to write a query in order to read the identity.

### Install the necessary dependencies
```bash npm2yarn
npm install graphql graphql-request
```

### Initialize the GraphQL Client
```jsx title="/src/queries/GetIdentity.tsx"
const client = new GraphQLClient(CYBERCONNECT_ENDPOINT);
```

### Write the query

```jsx title="/src/queries/GetIdentity.tsx"
export const GET_IDENTITY = gql`
    query($address: String!) {
        identity(address: $address) {
            domain
            followerCount
            followingCount
        }
    }
`;
```

You can test or write your own query in our [Playground](/cyberconnect-api/playground/) or simply make changes in the sandbox below:

<iframe src="https://codesandbox.io/embed/read-identity-2zjn9k?codemirror=1&fontsize=14&hidenavigation=0&theme=dark&runonclick=1&view=split"
    title="read-identity"
    allow="accelerometer; ambient-light-sensor; camera; encrypted-media; geolocation; gyroscope; hid; microphone; midi; payment; usb; vr; xr-spatial-tracking"
    sandbox="allow-forms allow-modals allow-popups allow-presentation allow-same-origin allow-scripts"
></iframe>

Make sure to check out the full list of fields available in the [CyberConnect API](/cyberconnect-api/overview/).
