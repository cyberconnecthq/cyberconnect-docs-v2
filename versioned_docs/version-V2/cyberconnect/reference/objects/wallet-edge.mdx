---
id: wallet-edge
title: WalletEdge
---


The `WalletEdge` type provides an `Wallet` node along with its cursor.

```graphql
type WalletEdge {
  node: Wallet
  cursor: Cursor!
}
```


### Fields

#### `node` ([`Wallet`](/cyberconnect/reference/objects/wallet))

`node` the node of the `Wallet` object.


#### `cursor` ([`Cursor`](/cyberconnect/reference/scalars/cursor))

`cursor` the cursor of the `Wallet` node.




