---
id: wallet-connection
title: WalletConnection
---


The `WalletConnection` type provides a paginated list of `WalletEdge` along with the total count of profiles.

```graphql
type WalletConnection {
  totalCount: Int!
  pageInfo: PageInfo!
  edges: [WalletEdge]
}
```


### Fields

#### `totalCount` ([`Int`](/cyberconnect/reference/scalars/int))

`totalCount` the total number of wallets.


#### `pageInfo` ([`PageInfo`](/cyberconnect/reference/objects/page-info))

`pageInfo` contains information about the current page.


#### `edges` ([`WalletEdge`](/cyberconnect/reference/objects/wallet-edge))

`edges` list of `WalletEdge` objects.




