---
id: profile
title: Profile
---


The `Profile` type provides information about a profile.

```graphql
type Profile implements Node {
  id: ID!
  profileID: Int!
  handle: String!
  owner: Wallet!
  metadata: URL
  metadataInfo: MetadataDetail
  avatar: URL
  externalMetadataInfo: ExternalMetadataDetail
  mintingBlock: Int!
  isPrimary: Boolean!
  operators: [AddressEVM!]!
  essences(
  after: Cursor
  first: Int
  before: Cursor
  last: Int
  orderBy: EssenceOrder
): EssenceConnection!
  subscribers(
  after: Cursor
  first: Int
  before: Cursor
  last: Int
  orderBy: SubscribeOrder
): SubscribeConnection!
  subscribeNFT: ContractAddressEVM
  subscribeMw: MiddlewareInfo
  namespace: Namespace!
  subscriberCount: Int!
  isSubscribedByMe(
  me: AddressEVM!
): Boolean!
}
```


### Fields

#### `id` ([`ID`](/cyberconnect/reference/scalars/id))

`id` the profile's unique identifier.


#### `profileID` ([`Int`](/cyberconnect/reference/scalars/int))

`profileID` the profile's token id.


#### `handle` ([`String`](/cyberconnect/reference/scalars/string))

`handle` the profile's handle.


#### `owner` ([`Wallet`](/cyberconnect/reference/objects/wallet))

`owner` the profile's owner wallet.


#### `metadata` ([`URL`](/cyberconnect/reference/scalars/url))

`metadata` the profile's metadata url.


#### `metadataInfo` ([`MetadataDetail`](/cyberconnect/reference/objects/metadata-detail))

`metadataInfo` information about the profile's metadata.


#### `avatar` ([`URL`](/cyberconnect/reference/scalars/url))

`avatar` the profile's avatar url.


#### `externalMetadataInfo` ([`ExternalMetadataDetail`](/cyberconnect/reference/objects/external-metadata-detail))

`externalMetadataInfo` informantion about the profile's metadata on Link3.


#### `mintingBlock` ([`Int`](/cyberconnect/reference/scalars/int))

`mintingBlock` the block number when the profile was minted.


#### `isPrimary` ([`Boolean`](/cyberconnect/reference/scalars/boolean))

`isPrimary` whether the profile is the primary profile of the owner.


#### `operators` ([`AddressEVM`](/cyberconnect/reference/scalars/address-evm))

`operators` the profile's list of operator addresses


#### `essences` ([`EssenceConnection`](/cyberconnect/reference/objects/essence-connection))

`essences` provides a paginated list of ordered essences.
- ##### `after` ([`Cursor`](/cyberconnect/reference/scalars/cursor))

`after` the cursor to retreive results after.


- ##### `first` ([`Int`](/cyberconnect/reference/scalars/int))

`first` the maximum number of essences to return after a cursor.


- ##### `before` ([`Cursor`](/cyberconnect/reference/scalars/cursor))

`before` the cursor to retreive results before.


- ##### `last` ([`Int`](/cyberconnect/reference/scalars/int))

`last` the maximum number of essences to return before a cursor.


- ##### `orderBy` ([`EssenceOrder`](/cyberconnect/reference/inputs/essence-order))

`orderBy` the order in which results are returned.


#### `subscribers` ([`SubscribeConnection`](/cyberconnect/reference/objects/subscribe-connection))

`subscribers` provides a paginated list of ordered subscribers.
- ##### `after` ([`Cursor`](/cyberconnect/reference/scalars/cursor))

`after` the cursor to retreive results after.


- ##### `first` ([`Int`](/cyberconnect/reference/scalars/int))

`first` the maximum number of subscribers to return after a cursor.


- ##### `before` ([`Cursor`](/cyberconnect/reference/scalars/cursor))

`before` the cursor to retreive results before.


- ##### `last` ([`Int`](/cyberconnect/reference/scalars/int))

`last` the maximum number of subscribers to return before a cursor.


- ##### `orderBy` ([`SubscribeOrder`](/cyberconnect/reference/inputs/subscribe-order))

`orderBy` the order in which results are returned.


#### `subscribeNFT` ([`ContractAddressEVM`](/cyberconnect/reference/scalars/contract-address-evm))

`subscribeNFT` the profile's subscribe NFT contract address.


#### `subscribeMw` ([`MiddlewareInfo`](/cyberconnect/reference/objects/middleware-info))

`subscribeMw` information about the profile's subscribe middleware.


#### `namespace` ([`Namespace`](/cyberconnect/reference/objects/namespace))

`namespace` information about the profile's namespace.


#### `subscriberCount` ([`Int`](/cyberconnect/reference/scalars/int))

`subscriberCount` the number of subscribers.


#### `isSubscribedByMe` ([`Boolean`](/cyberconnect/reference/scalars/boolean))

`isSubscribedByMe` whether the given address has subscribed to the profile.
- ##### `me` ([`AddressEVM`](/cyberconnect/reference/scalars/address-evm))




### Interfaces

#### [`Node`](/cyberconnect/reference/interfaces/node) 

The `Node` interface follows relay.dev spec.




